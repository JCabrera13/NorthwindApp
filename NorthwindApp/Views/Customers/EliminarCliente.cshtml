@model NorthwindApp.Models.Customer

@{
    ViewData["Title"] = "Eliminar";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    @*se enlistan los valores de la propiedad*@
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CustomerId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CustomerId)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CompanyName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CompanyName)
        </dd>
    </dl>

    @*recibe mediante post.*@
    @*form para que el usuario pueda eliminar*@
    <form asp-action="ConfirmacionEliminarCliente">
        @*campo escondido para la propiedad*@
        <input type="hidden" asp-for="CustomerId" />
        <input type="submit" value="Eliminar" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
